Created At: 2026-08-11T00:08:47+03:00
Completed At: 2026-08-11T00:08:47+03:00
The following changes were made by the replace_file_content tool to: /Users/dipencil/Development/legendary-management-mea-website/app/globals.css. If relevant, proactively run terminal commands to execute this code for the USER. Don't ask for permission.
[diff_block_start]
@@ -1205,14 +1205,24 @@
 }
 .grid-glow {
   position: absolute;
-  top: 30%;
-  left: 50%;
-  transform: translate(-50%, -50%);
-  width: 600px;
-  height: 600px;
-  background: radial-gradient(circle, rgba(8, 29, 96, 0.08) 0%, transparent 70%);
-  filter: blur(40px);
+  filter: blur(50px);
   z-index: -1;
+  border-radius: 50%;
+}
+.grid-glow-navy {
+  top: 10%;
+  left: 50%;
+  transform: translateX(-50%);
+  width: 700px;
+  height: 700px;
+  background: rgba(8, 29, 96, 0.12);
+}
+.grid-glow-gold {
+  bottom: 5%;
+  left: 20%;
+  width: 500px;
+  height: 500px;
+  background: rgba(160, 127, 49, 0.08);
 }
 .grid-beam-mask {
   position: absolute;
@@ -1219,5 +1219,5 @@
-  mask-image: radial-gradient(ellipse at center 70%, black 15%, transparent 75%);
-  -webkit-mask-image: radial-gradient(ellipse at center 70%, black 15%, transparent 75%);
+  mask-image: radial-gradient(ellipse at center 60%, black 40%, transparent 90%);
+  -webkit-mask-image: radial-gradient(ellipse at center 60%, black 40%, transparent 90%);
 }
 .grid-pattern {
   position: absolute;
@@ -1224,8 +1224,8 @@
   background-position: center top;
   background-size: clamp(48px, 5vw, 72px) clamp(48px, 5vw, 72px);
   background-image: 
-    linear-gradient(to right, rgba(8, 29, 96, 0.12) 1px, transparent 1px),
-    linear-gradient(to bottom, rgba(8, 29, 96, 0.12) 1px, transparent 1px);
+    linear-gradient(to right, rgba(8, 29, 96, 0.18) 1px, transparent 1px),
+    linear-gradient(to bottom, rgba(8, 29, 96, 0.18) 1px, transparent 1px);
 }
 .grid-beam {
   position: absolute;
@@ -1232,8 +1232,8 @@
   left: 50%;
   width: 2px;
   height: 40vh;
-  background: linear-gradient(to bottom, transparent, rgba(160, 127, 49, 0.7) 95%, rgba(160, 127, 49, 1));
-  box-shadow: 0 0 8px 1px rgba(160, 127, 49, 0.18);
+  background: linear-gradient(to bottom, transparent, rgba(160, 127, 49, 0.85) 95%, rgba(160, 127, 49, 1));
+  box-shadow: 0 0 10px 2px rgba(160, 127, 49, 0.28);
   animation: gridBeamMove 14s linear infinite;
 }
 .grid-beam::after {
@@ -1241,11 +1241,11 @@
   bottom: 0;
   left: 50%;
   transform: translateX(-50%);
-  width: 6px;
-  height: 6px;
+  width: 8px;
+  height: 8px;
   background-color: rgba(160, 127, 49, 1);
   border-radius: 50%;
-  box-shadow: 0 0 14px 4px rgba(160, 127, 49, 0.4);
+  box-shadow: 0 0 16px 6px rgba(160, 127, 49, 0.24);
 }
 @keyframes gridBeamMove {
   0% { transform: translateY(-60vh); opacity: 0; }
[diff_block_end]

Please note that the above snippet only shows the MODIFIED lines from the last change. It shows up to 3 lines of unchanged lines before and after the modified lines. The actual file contents may have many more lines not shown.