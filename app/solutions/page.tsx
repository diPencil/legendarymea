'use client'
import { ArrowButton, ContactBlock, FAQ, PageShell, SectionTitle, images } from '@/components/site'
import { useContent } from '@/components/i18n'
import { Reveal, StaggerGrid } from '@/components/motion'

export default function SolutionsPage() { const c = useContent(); return <PageShell><Reveal><section className="inner-hero section-shell"><div><div className="section-kicker">{c.page.services}</div><h1>{c.page.servicesTitle}<br /><em>{c.page.servicesAccent}</em></h1><p>{c.servicesBody}</p></div><div className="inner-image"><img src={images.travel} alt="Business traveler in a premium airport lounge" /></div></section></Reveal><section className="dark-panel section-shell"><SectionTitle kicker="01 / Ecosystem" light>{c.servicesTitle}</SectionTitle><StaggerGrid className="service-grid">{c.services.map(([title, desc], i) => <article className="service-card" key={title}><span className="service-index">0{i + 1}</span><div><h3>{title}</h3><p>{desc}</p></div><ArrowButton label="Explore" href={`/solutions/${['hotels-accommodation','flights','transfers','car-rental','tours-experiences','groups-special-requests','corporate-travel','hospitality-solutions'][i]}`} /></article>)}</StaggerGrid></section><section className="regional section-shell"><div><div className="section-kicker">02 / Commercial clarity</div><h2>One brief.<br /><em>Many possibilities.</em></h2><p>Bring us the destination, dates, audience and ambition. We connect the moving parts into a practical response your team can trust.</p><ArrowButton label="Start a request" /></div><div className="regional-image"><img src={images.meeting} alt="Travel professionals collaborating" /></div></section><FAQ /><ContactBlock /></PageShell> }
