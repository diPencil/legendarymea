Created At: 2026-08-10T06:27:56+03:00
Completed At: 2026-08-10T06:27:56+03:00
The following changes were made by the multi_replace_file_content tool to: /Users/dipencil/Development/legendary-management-mea-website/app/globals.css. If relevant, proactively run terminal commands to execute this code for the USER. Don't ask for permission.
[diff_block_start]
@@ -714,20 +714,12 @@
   margin-inline-start: 5px;
   color: var(--muted);
 }
-.audience-editorial {
+.audience-banner {
   padding: 80px 0 100px;
   border-top: 1px solid var(--line);
 }
-.audience-editorial-inner {
-  display: grid;
-  grid-template-columns: 1fr;
-  gap: 50px;
-}
-@media (min-width: 1024px) {
-  .audience-editorial-inner {
-    grid-template-columns: 35% 1fr;
-    gap: 8vw;
-  }
+.audience-heading-block {
+  margin-bottom: 50px;
 }
 .audience-heading-block h2 {
   font-size: clamp(34px, 3.5vw, 42px);
@@ -734,66 +734,194 @@
   letter-spacing: -0.05em;
   margin: 24px 0 15px;
-}
-.audience-rows {
-  display: flex;
-  flex-direction: column;
-}
-.audience-row {
-  display: flex;
-  align-items: center;
-  gap: 20px;
-  padding: 24px 0;
-  border-bottom: 1px solid var(--line);
-  cursor: pointer;
-  transition: border-color 0.3s ease;
-}
-.audience-row:first-child {
-  border-top: 1px solid var(--line);
-}
-.row-index {
-  font-size: 14px;
-  font-weight: 500;
-  color: var(--muted);
-  width: 25px;
-  transition: color 0.3s ease;
-}
-.row-content {
-  flex: 1;
-}
-.row-content h3 {
-  font-size: clamp(20px, 2vw, 26px);
-  margin: 0 0 6px 0;
-  font-weight: 600;
-  transition: transform 0.3s ease;
-}
-.row-content p {
-  margin: 0;
-  font-size: clamp(14px, 1.5vw, 16px);
-  color: var(--muted);
-}
-.row-arrow {
-  color: var(--muted);
-  transition: transform 0.3s ease, color 0.3s ease;
-}
-.audience-row:hover .row-index {
-  color: var(--gold);
-}
-.audience-row:hover .row-content h3 {
-  transform: translateX(4px);
-}
-[dir="rtl"] .audience-row:hover .row-content h3 {
-  transform: translateX(-4px);
-}
-.audience-row:hover .row-arrow {
-  color: var(--gold);
-  transform: translateX(6px) translateY(-6px);
-}
-[dir="rtl"] .audience-row:hover .row-arrow {
-  transform: translateX(-6px) translateY(-6px);
-}
-.audience-row:hover {
-  border-bottom-color: var(--ink);
-}
+  color: var(--navy);
+  max-width: 600px;
+}
+
+/* Horizontal Interactive Banner */
+.audience-interactive-strip {
+  display: flex;
+  flex-direction: column;
+  width: 100%;
+  border: 1px solid var(--line);
+  border-radius: 24px;
+  background: var(--paper);
+  overflow: hidden;
+}
+
+@media (min-width: 1024px) {
+  .audience-interactive-strip {
+    flex-direction: row;
+    border-radius: 0;
+    border-left: none;
+    border-right: none;
+    background: transparent;
+  }
+}
+
+.audience-item {
+  position: relative;
+  padding: 30px;
+  border-bottom: 1px solid var(--line);
+  cursor: pointer;
+  outline: none;
+  transition: flex 0.6s cubic-bezier(0.25, 1, 0.5, 1), background 0.4s ease, opacity 0.4s ease;
+}
+
+@media (min-width: 1024px) {
+  .audience-item {
+    flex: 1;
+    border-bottom: none;
+    border-inline-end: 1px solid var(--line);
+    padding: 40px 30px;
+  }
+  .audience-item:last-child {
+    border-inline-end: none;
+  }
+  .audience-interactive-strip:hover .audience-item {
+    flex: 0.8;
+    opacity: 0.5;
+  }
+  .audience-interactive-strip:hover .audience-item:hover,
+  .audience-interactive-strip:focus-within .audience-item:focus-visible {
+    flex: 2;
+    opacity: 1;
+    background: #fff;
+  }
+}
+
+.audience-item-inner {
+  display: flex;
+  flex-direction: column;
+  justify-content: center;
+  height: 100%;
+  position: relative;
+}
+
+.audience-item-header {
+  display: flex;
+  flex-direction: column;
+  gap: 12px;
+  transition: transform 0.6s cubic-bezier(0.25, 1, 0.5, 1);
+}
+
+@media (min-width: 1024px) {
+  .audience-item-header {
+    transform: translateY(20px);
+  }
+  .audience-item:hover .audience-item-header,
+  .audience-item:focus-visible .audience-item-header {
+    transform: translateY(0);
+  }
+}
+
+.audience-index {
+  font-size: 14px;
+  font-weight: 500;
+  color: var(--muted);
+  transition: color 0.4s ease;
+}
+
+.audience-item h3 {
+  font-size: clamp(20px, 1.8vw, 28px);
+  font-weight: 600;
+  margin: 0;
+  color: var(--navy);
+  line-height: 1.2;
+}
+
+.audience-reveal {
+  display: flex;
+  justify-content: space-between;
+  align-items: flex-end;
+  margin-top: 20px;
+  gap: 20px;
+  opacity: 1;
+  max-height: 150px;
+  transition: all 0.6s cubic-bezier(0.25, 1, 0.5, 1);
+}
+
+@media (min-width: 1024px) {
+  .audience-reveal {
+    opacity: 0;
+    max-height: 0;
+    transform: translateY(10px);
+    margin-top: 0;
+  }
+  .audience-item:hover .audience-reveal,
+  .audience-item:focus-visible .audience-reveal {
+    opacity: 1;
+    max-height: 150px;
+    transform: translateY(0);
+    margin-top: 20px;
+  }
+}
+
+.audience-reveal p {
+  margin: 0;
+  font-size: clamp(14px, 1.1vw, 15px);
+  color: var(--muted);
+  max-width: 85%;
+  line-height: 1.5;
+}
+
+.audience-arrow {
+  color: var(--muted);
+  transition: color 0.4s ease, transform 0.4s ease;
+}
+
+.audience-item:hover .audience-index,
+.audience-item:focus-visible .audience-index {
+  color: var(--gold);
+}
+
+.audience-item:hover .audience-arrow,
+.audience-item:focus-visible .audience-arrow {
+  color: var(--gold);
+}
+@media (min-width: 1024px) {
+  .audience-item:hover .audience-arrow,
+  .audience-item:focus-visible .audience-arrow {
+    transform: translateX(4px) translateY(-4px);
+  }
+  [dir="rtl"] .audience-item:hover .audience-arrow,
+  [dir="rtl"] .audience-item:focus-visible .audience-arrow {
+    transform: translateX(-4px) translateY(-4px);
+  }
+}
+
+/* Mobile Scrollable Strip */
+@media (max-width: 1023px) {
+  .audience-interactive-strip {
+    flex-direction: row;
+    overflow-x: auto;
+    scroll-snap-type: x mandatory;
+    scroll-padding: 20px;
+    padding-bottom: 20px;
+    border: none;
+    border-radius: 0;
+    background: transparent;
+    margin-left: -24px;
+    margin-right: -24px;
+    padding-left: 24px;
+    padding-right: 24px;
+    gap: 15px;
+  }
+  .audience-item {
+    flex: 0 0 85%;
+    scroll-snap-align: center;
+    border: 1px solid var(--line);
+    border-radius: 20px;
+    background: var(--paper);
+    padding: 30px;
+  }
+  [dir="rtl"] .audience-interactive-strip {
+    margin-left: -24px;
+    margin-right: -24px;
+    padding-left: 24px;
+    padding-right: 24px;
+  }
+}
+
 .mini-pills span {
   display: inline-flex;
   align-items: center;
[diff_block_end]

Please note that the above snippet only shows the MODIFIED lines from the last change. It shows up to 3 lines of unchanged lines before and after the modified lines. The actual file contents may have many more lines not shown.